{
  "name": "miro-canvas",
  "version": "0.1.0",
  "private": true,
  "description": "Offline native Canvas extensions for Obsidian.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs --production",
    "dev": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.8"
  }
}
